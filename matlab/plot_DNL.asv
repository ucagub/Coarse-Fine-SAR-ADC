function y = plot_DNL(DAC)
%plots the DNL of a DAC
%usage : plot_DNL(@DAC) where DAC is the DAC function
    dnl = [];
    prev = 0;
    for i = 0:255
        dnl = [dnl DNL(DAC(i), prev)];
        prev = DAC(i);
    end
    y = DAC(1);
end